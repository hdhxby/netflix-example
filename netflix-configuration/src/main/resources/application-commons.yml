spring:
    main:
        allow-bean-definition-overriding: true
    profiles:
        include:
            - logging
            - zipkin
management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        health:
            show-details: ALWAYS
